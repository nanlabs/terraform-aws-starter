# serverless-plugin-typescript@2 peers on serverless 2||3 while the project
# deploys with serverless v4 (upstream unmaintained, no v4-compatible release).
# The committed lockfile encodes the legacy resolution; keep installs reproducible.
legacy-peer-deps=true
